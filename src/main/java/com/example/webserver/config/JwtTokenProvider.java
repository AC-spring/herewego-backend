package com.example.webserver.config; // SecurityConfig와 같은 패키지에 저장

import org.springframework.stereotype.Component;

@Component
public class JwtTokenProvider {
    // 실제 JWT 생성, 검증, 사용자 정보 추출 로직은 여기에 구현해야 합니다.
}